/*
*/

#ifndef MY_CAMERA_H
#define MY_CAMERA_H

#include <stdio.h>
#include <SDL.h>
#include <gl/glew.h>
#include <SDL_opengl.h>

class Camera {
public:
    Camera();
    ~Camera();
protected:
private:
};

#endif
